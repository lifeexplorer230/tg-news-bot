# ↩️ ROLLBACK PROTOCOL
## Процедуры отката при инцидентах

**Версия:** 1.0
**Дата:** 2025-10-11

---

## 🚨 КОГДА ОТКАТЫВАТЬ?

### Критичные ситуации (откат немедленно)

- 🔴 **AttributeError / ImportError** после изменений
- 🔴 **Database corruption** или ошибки миграции
- 🔴 **Потеря данных** (сообщения/публикации)
- 🔴 **Бот не запускается** после изменений

### Некритичные ситуации (можно попытаться исправить)

- 🟡 **Тесты не проходят** (но код работает)
- 🟡 **Большой diff size** (но изменения корректны)
- 🟡 **Логирование не работает** (но функциональность ок)

---

## 🔄 УРОВНИ ОТКАТА

### Уровень 1: Откат uncommitted изменений

**Когда использовать:** Изменения ещё не закоммичены, но что-то пошло не так.

```bash
# Посмотреть что изменено
git status
git diff

# Откатить все изменения
git checkout -- .

# ИЛИ откатить конкретный файл
git checkout -- <файл>

# Проверить что откат прошёл
git status  # Должно быть: nothing to commit, working tree clean

# Записать в лог
echo "[$(date)] ОТКАТ uncommitted изменений для <файл/задача>" >> logs/migration_$(date +%Y-%m-%d).log
```

✅ **Результат:** Вернулись к последнему коммиту

---

### Уровень 2: Откат последнего коммита (локально)

**Когда использовать:** Коммит создан, но ещё не запушен, нужно откатить.

```bash
# Посмотреть последние коммиты
git log --oneline -5

# ВАРИАНТ A: Откат с сохранением изменений (soft reset)
git reset --soft HEAD~1

# Изменения останутся, можно исправить и закоммитить заново
git status

# ВАРИАНТ B: Откат с удалением изменений (hard reset)
git reset --hard HEAD~1

# Изменения удалены, вернулись к предыдущему коммиту
git log --oneline -5

# Записать в лог
echo "[$(date)] ОТКАТ последнего коммита (reset)" >> logs/migration_$(date +%Y-%m-%d).log
```

⚠️ **ВНИМАНИЕ:** `git reset --hard` удаляет изменения безвозвратно!

✅ **Результат:** Последний коммит отменён

---

### Уровень 3: Откат запушенного коммита (revert)

**Когда использовать:** Коммит уже в remote, нужно откатить безопасно.

```bash
# Посмотреть коммиты
git log --oneline -10

# Найти хеш коммита для отката (например: abc123)

# Создать revert коммит (безопасно)
git revert abc123

# Git создаст новый коммит с откатом изменений
# Редактор откроется для сообщения коммита

# Или без редактора:
git revert --no-edit abc123

# Push revert коммита
git push origin <текущая_ветка>

# Записать в лог
echo "[$(date)] REVERT коммита abc123" >> logs/migration_$(date +%Y-%m-%d).log
```

✅ **Результат:** Изменения откачены через новый коммит (история сохранена)

---

### Уровень 4: Откат к определённой точке (checkpoint)

**Когда использовать:** Нужно вернуться к состоянию после конкретного этапа.

```bash
# Найти коммит-checkpoint
git log --oneline --grep="[B3]"  # Найти коммит этапа B3

# Создать новую ветку от checkpoint (для безопасности)
git checkout -b rollback-to-b3 <хеш_коммита>

# ИЛИ hard reset к checkpoint (опасно!)
git reset --hard <хеш_коммита>
git push origin <текущая_ветка> --force  # ОПАСНО!

# Записать в лог
echo "[$(date)] ОТКАТ к checkpoint <хеш>" >> logs/migration_$(date +%Y-%m-%d).log
```

⚠️ **ВНИМАНИЕ:** Force push опасен, использовать только в крайнем случае!

✅ **Результат:** Вернулись к состоянию checkpoint

---

### Уровень 5: Откат схемы БД

**Когда использовать:** Миграция БД пошла не так.

```bash
# Если добавляли поле в таблицу
sqlite3 ./data/marketplace_news.db "ALTER TABLE raw_messages DROP COLUMN <имя_поля>;"

# Если создавали новую таблицу
sqlite3 ./data/marketplace_news.db "DROP TABLE IF EXISTS <имя_таблицы>;"

# Проверить схему
sqlite3 ./data/marketplace_news.db ".schema raw_messages"

# ИЛИ восстановить из backup
cp ./data/backup/marketplace_news_YYYY-MM-DD.db ./data/marketplace_news.db

# Записать в лог
echo "[$(date)] ОТКАТ схемы БД" >> logs/migration_$(date +%Y-%m-%d).log
```

⚠️ **ВАЖНО:** Всегда делать backup БД перед миграцией!

✅ **Результат:** Схема БД восстановлена

---

## 📋 ЧЕК-ЛИСТ: ПРОЦЕДУРА ОТКАТА

### Перед откатом

- [ ] Записать текущую ситуацию в лог
- [ ] Сделать snapshot текущего состояния (если нужно)
- [ ] Определить уровень отката
- [ ] Убедиться что понятна причина проблемы

### Во время отката

- [ ] Выполнить команды отката
- [ ] Проверить git status
- [ ] Проверить что код работает
- [ ] Проверить БД (если были изменения)

### После отката

- [ ] Записать факт отката в лог
- [ ] Обновить DOROZHNAYA_KARTA.md (вернуть статус)
- [ ] Обновить memory-bank/diff-limit-plan.md (если это инцидент)
- [ ] Проанализировать причину
- [ ] Составить план исправления

---

## 🎯 МАТРИЦА ПРИНЯТИЯ РЕШЕНИЯ

| Проблема | Уровень отката | Команда |
|----------|----------------|---------|
| Синтаксическая ошибка | 1 (uncommitted) | `git checkout -- <файл>` |
| Импорт не работает | 1-2 | `git checkout --` или `git reset` |
| Тесты упали | 2 (если коммит) | `git reset --soft HEAD~1` |
| Коммит запушен, баг | 3 (revert) | `git revert <хеш>` |
| Миграция БД сломалась | 5 (БД) + 2/3 (код) | Restore backup + reset |
| Критический баг в prod | 4 (checkpoint) | `git reset --hard <хеш>` |

---

## 📝 ШАБЛОН ОТЧЁТА ОБ ОТКАТЕ

Копируйте и заполняйте:

```
### ОТКАТ [КОД_ЗАДАЧИ] - YYYY-MM-DD HH:MM

**Проблема:** <описание>
**Уровень отката:** <1-5>
**Команды:**
```bash
<выполненные команды>
```

**Результат:** <описание результата>
**Причина проблемы:** <анализ>
**План исправления:** <что будем делать дальше>
**Обновлены документы:**
- [ ] logs/migration_YYYY-MM-DD.log
- [ ] DOROZHNAYA_KARTA.md
- [ ] memory-bank/diff-limit-plan.md
```

Сохранить в: `logs/rollback_YYYY-MM-DD_HH-MM.log`

---

## 🚀 ПОСЛЕ ОТКАТА: ПЛАН ДЕЙСТВИЙ

1. **Анализ:**
   - Что пошло не так?
   - Почему не заметили раньше?
   - Какие проверки пропустили?

2. **Исправление:**
   - Как правильно реализовать?
   - Разбить на меньшие партии?
   - Добавить проверки?

3. **Профилактика:**
   - Обновить verification-protocol.md
   - Обновить diff-limit-plan.md
   - Добавить в common-issues.md

4. **Повторная попытка:**
   - Составить детальный план
   - Выполнить с усиленными проверками
   - Записать успешный результат

---

## 📊 СТАТИСТИКА ОТКАТОВ

_Будет заполняться по мере выполнения миграции_

| Дата | Задача | Проблема | Уровень | Решение |
|------|--------|----------|---------|---------|
| _TBD_ | — | — | — | — |

**Цель:** ≤ 3 отката за всю миграцию

---

**Версия:** 1.0
**Последнее обновление:** 2025-10-11
