# 🗺️ ДОРОЖНАЯ КАРТА МИГРАЦИИ: УНИВЕРСАЛЬНЫЙ ДАЙДЖЕСТ-БОТ

**Версия:** 3.0.0  
**Дата запуска цикла:** 2025-10-13  
**Статус:** 🟡 Активна

---

## 📌 Контекст и цели

| Аспект | Описание |
|--------|----------|
| Наследие | Marketplace News Bot обслуживает маркетплейс-дайджесты и содержит встроенные допущения по источникам, категориям и публикациям. |
| Запрос | Требуется адаптировать кодовую базу под универсальный сценарий: любой контейнер + новая конфигурация → бот выпускает дайджест по новому домену (например, AI-новости). |
| Ограничения | Минимальные изменения в существующих сервисах, сохранение стабильности prod (marketplace-профиль), отсутствие сети в CI, SQLite как основной storage. |
| Цель | Ввести конфигурационные профили, позволяющие переключать Telegram-аккаунты, каналы, промпты и расписания без правок кода, и подготовить инфраструктуру «roadmap + protocol + memory-bank». |

---

## 🎯 Основные задачи

1. **Унификация конфигурации** — единый загрузчик профилей (base + profile), отдельные файлы для marketplace, ai и будущих сценариев.
2. **Параметризация listener/processor** — все фильтры, промпты и квоты приходят из профиля; код читает только абстрактные параметры.
3. **Универсальный модерационный поток** — инструкции, количество исключаемых новостей, таймауты и чаты управляются конфигурацией.
4. **Документация и протоколы** — обновлённая дорожная карта, инструкции, memory-bank и diff-limit регламентируют партии и проверки.
5. **Стабильность и контроль** — после каждого этапа откатные точки, сохранение baseline в `snapshot/` и логов в `logs/` (в git — только `.gitkeep`).

---

## ⚖️ Сравнение вариантов

| Критерий | Вариант A: точечные правки | Вариант B: конфиг-профили (выбран) | Вариант C: форк и новый проект |
|----------|---------------------------|------------------------------------|--------------------------------|
| Время запуска нового профиля | Высокое (ручные правки) | Низкое (копия YAML) | Среднее |
| Риск регрессий | Высокий | Средний (нужны тесты) | Высокий (дублирование) |
| Повторное использование | Низкое | Высокое | Низкое |
| Сложность поддержки | Растёт с каждым профилем | Регулируется конфигами | Дублирование багов |
| Вывод | Не подходит | ✅ Выбран | Не подходит |

---

## 🧭 Подход

- **Минимальные партии** — следуем `memory-bank/diff-limit-plan.md`, безопасная граница 150 строк; escalation через «обстукивание туннеля».
- **Двойная проверка** — после каждой партии обязательны `git diff --stat`, ревизия инструкций и обновление журнала.
- **Точки отката** — на стыке этапов A–E создаём снапшоты в `snapshot/` (только локально), фиксируем контрольные теги/коммиты.
- **Контроль диффов** — каждое подзадача → отдельный коммит, в PR попадает линейная история; после инцидента уменьшаем партию и обновляем diff-plan.
- **Артефакты вне git** — реальные логи, baseline и снапшоты живут в `logs/` и `snapshot/`; в репозитории оставляем `.gitkeep` и ссылки.
- **Протокол** — любая рабочая итерация стартует командой `Протокол, шаг <код>`, затем последовательность Memory → Roadmap → Instruction.

---

## 🔢 Этапы A–E

| Код | Название | Ключевые действия | Контрольные проверки |
|-----|----------|-------------------|----------------------|
| **A** | Инфраструктура профилей | загрузчик конфигов, структура `config/base.yaml` + `config/profiles/*`, указатели путей `data/logs/sessions` | Юнит-тест config loader, ручная проверка переключения параметров |
| **B** | Listener/DB параметризация | настройка каналов, фильтров, retry, вынесение таймаутов | Test `test_database.py`, dry-run listener в sandbox |
| **C** | Processor & LLM | промпты из конфигов, topN, exclude, категории, дедуп | Интеграционные тесты processor (mock Gemini), diff по шаблонам |
| **D** | Модерация и публикация | инструкции из профиля, таймауты, footer, уведомления | Ручной прогон `Протокол, шаг D1`, проверка логов |
| **E** | Документация и развёртывание | обновлённые инструкции, roadmap, PR, snapshot | Финальный чек-лист, smoke-тест профилей |

**Вложенные проверки:**
- A1 baseline, A2 загрузчик, A3 docker-volume обновление.
- B1 listener modes, B2 filters, B3 db retry покрытие.
- C1 промпт-движок, C2 duplicate threshold, C3 digest шаблон.
- D1 модерация, D2 публикация, D3 статус-репорт.
- E1 docs, E2 журнал, E3 PR & откат.

---

## ⚠️ Риски и меры

| Риск | Вероятность | Влияние | Митигирующие действия |
|------|-------------|---------|------------------------|
| Конфигурации перепутаются между профилями | Средняя | Высокое | Ввести явную команду `--profile`, добавить проверку в main и тесты ; чек-лист A3 |
| Блокировки SQLite из-за нескольких контейнеров | Высокая | Среднее | Применить retry (уже реализован) + отдельные файлы БД на профиль |
| Изменения промптов сломают маркетплейс-профиль | Средняя | Высокое | Хранить промпты по профилям, выполнять smoke-тест с marketplace YAML |
| Слишком большие партии в документации | Низкая | Среднее | Поддерживать diff-limit журнал, дробить обновления |
| Потеря baseline | Низкая | Высокое | Перед этапами сохранять `/snapshot/<дата>-<этап>/` локально |

---

## ✅ Чек-листы активных шагов

- **A1** Подготовка структуры профилей → см. `INSTRUKTSIYA_VYPOLNENIYA.md` / Этап A.  
- **B1** Настройка listener (manual/subscriptions) → убедиться в наличии тестовых каналов, обновить конфиг.  
- **C1** Промпты LLM → вынести в конфиг, добавить ссылку на файл.  
- **D1** Модерация → проверить команды «0», «отмена», «все», исключения.  
- **E1** Документация → синхронизировать roadmap, инструкцию, memory-bank.

Отметки по чек-листам вносим в раздел «Журнал прогресса» после каждой партии.

---

## 📓 Журнал прогресса

| Дата/время | Этап | Партия | Статус | Комментарий |
|------------|------|--------|--------|-------------|
| 2025-10-13 11:47 | A1 | Конфигурационная основа | ✅ | Созданы base/profiles, перенесена сессия в `sessions/marketplace/` |
| 2025-10-13 11:59 | A2 | Загрузчик профилей | ✅ | Реализовано объединение base+profile, argparse `--profile`, тесты `test_config_profiles` |
| 2025-10-13 12:08 | A3 | Docker/сессии | ✅ | Обновлён docker-compose (volumes, PROFILE), добавлен `.gitkeep` для sessions |
| 2025-10-13 12:27 | B1 | Listener режимы | ✅ | Добавлен manual режим, параметризация каналов, тесты `test_listener_manual` |
| 2025-10-13 12:45 | B2 | Listener фильтры | ✅ | Конфиги whitelist/blacklist, тест whitelists, полный `pytest -q` |
| 2025-10-13 12:46 | B3 | DB retry | ✅ | Параметры timeout/retry в конфиге, `Database` читает профили, обновлены тесты |
| 2025-10-13 14:20 | C1 | Gemini промпты | ✅ | Промпты вынесены в `config/prompts`, `GeminiClient` читает профили, добавлен тест лоадера |
| 2025-10-13 17:03 | D1 | Модерация | ✅ | Сообщения/инструкции параметризованы, учитывается `exclude_count` |
| 2025-10-13 17:05 | D2 | Публикация | ✅ | Параметризованы header/footer, превью и уведомления, шаблоны из профиля |
| 2025-10-13 17:06 | D3 | Статус-репорт | ✅ | Шаблон сообщения, fallback, unit-тесты для StatusReporter |

Заполняется командой «Протокол, шаг …» после завершения подэтапа/партии.

---

## 📌 Текущее состояние

- Marketplace-профиль продолжает работать в проде, миграция не начата.  
- Документы roadmap/protocol обновлены под новую методологию.  
- Ожидается запуск этапа A после подтверждения дорожной карты.

---

## 🛠 Быстрый протокол

1. **Команда:** `Протокол, шаг <код>` (например, `Протокол, шаг A1`).
2. **Memory:** просмотреть памятки в `memory-bank/` (diff-limit, ops, rollback).
3. **Roadmap:** свериться с настоящим файлом, уточнить подзадачу.
4. **Instruction:** открыть соответствующий раздел в `INSTRUKTSIYA_VYPOLNENIYA.md`.
5. **Действия:** выполнить чек-лист, обновить журналы, сохранить логи в `logs/` (в git — только `.gitkeep`).
6. **Отчёт:** зафиксировать результат в разделе «Журнал прогресса» и, при необходимости, в memory-bank.

---

_Этот документ — Single Source of Truth: любые изменения дорожки, рисков или подхода вносятся сюда до начала работ._

---

## 🔄 План отката / PR

- **Последний стабильный коммит:** `b5f78b2561ca9893ed6cb79582683fde42afbe66` (`snapshot/baseline_commit.txt`)
- **Rollback:** при необходимости выполнить `git checkout b5f78b2561ca9893ed6cb79582683fde42afbe66`
- **Проверки перед PR:** `pytest -q`
- **Pull Request:** включить описание этапов A1–D3, указать новые файлы из `config/prompts/`, напомнить о необходимости обновить `.env`
