# 📱 NEWS-TO-REELS GENERATOR

**Версия:** 1.0.0
**Статус:** 🔨 In Development
**Дата создания:** 2025-10-20
**Интеграция:** Модуль для TG News Bot

---

## 📌 ОБЗОР ПРОЕКТА

**News-to-Reels Generator** — модуль для автоматической генерации сценариев Instagram Reels (30 секунд) из новостей с использованием Perplexity API.

### Ключевые возможности

- 📰 **Обогащение новостей** — получение дополнительного контекста, фактов, предыстории через Perplexity
- 🎬 **Генерация сценариев** — автоматическое создание structure для 30-секундных видео
- 🔄 **Интеграция с ТНБ** — использует обработанные новости из базы данных TG News Bot
- 📲 **Модерация в Telegram** — отправка сценариев для review перед использованием
- 🎯 **Гибкие промпты** — настраиваемые шаблоны для разных стилей контента

---

## 🏗️ АРХИТЕКТУРА

### Интеграция в TG News Bot

```
tg-news-bot/
├── reels/                           # ← НОВЫЙ МОДУЛЬ
│   ├── __init__.py
│   ├── main.py                      # Точка входа для reels режима
│   │
│   ├── models/
│   │   ├── __init__.py
│   │   ├── news.py                  # News, EnrichedNews models
│   │   └── reels.py                 # ReelsScenario model
│   │
│   ├── services/
│   │   ├── __init__.py
│   │   ├── perplexity_client.py     # Perplexity API client
│   │   └── reels_processor.py       # News → Reels processing logic
│   │
│   ├── config/
│   │   ├── __init__.py
│   │   └── reels_config.py          # Perplexity settings, prompts config
│   │
│   ├── prompts/
│   │   ├── enrich_news.md           # Промпт для обогащения новостей
│   │   └── generate_reels.md        # Промпт для генерации сценария
│   │
│   ├── tests/
│   │   ├── test_perplexity_client.py
│   │   └── test_reels_processor.py
│   │
│   └── PROJECT_DESCRIPTION.md       # Этот файл
│
├── config/profiles/
│   └── reels.yaml                   # ← НОВЫЙ ПРОФИЛЬ
│
└── main.py                          # ← ДОБАВИТЬ РЕЖИМ "reels"

ПЕРЕИСПОЛЬЗУЕТСЯ ИЗ ТНБ:
├── database/                        # База данных (получение новостей)
├── utils/logger.py                  # Логирование
├── utils/config.py                  # Конфигурация
├── services/telegram_listener.py    # Telegram integration (для модерации)
└── .env                            # + PERPLEXITY_API_KEY
```

### Модульный подход

- **Слабая связанность**: Модуль можно легко выделить в отдельный проект
- **Переиспользование инфраструктуры**: БД, логирование, конфигурация
- **Независимая разработка**: Можно тестировать отдельно от основного бота

---

## 🔄 WORKFLOW

```
┌─────────────────────────────────────────────────────────────┐
│  1. TG News Bot собирает и обрабатывает новости             │
│     (listener → processor → БД)                             │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────────┐
│  2. Reels Generator запускается:                            │
│     - python main.py reels --profile reels (ручной)        │
│     - Автоматически после processor (если включено)         │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────────┐
│  3. ReelsProcessor получает топ-новости из БД               │
│     SELECT * FROM published WHERE date = today LIMIT N      │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────────┐
│  4. Для каждой новости → Perplexity API:                    │
│     a) Обогащение (дополнительный контекст, факты)          │
│     b) Генерация сценария Reels (hook, content, CTA)        │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────────┐
│  5. Форматирование результатов для Telegram                 │
│     - Обогащенная новость                                   │
│     - Сценарий с хронометражем (0-3, 3-25, 25-30 сек)     │
│     - Визуальные рекомендации                               │
│     - Хэштеги, настроение музыки                            │
└─────────────────┬───────────────────────────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────────┐
│  6. Отправка в Telegram для модерации                       │
│     → тот же канал/аккаунт, что и дайджесты ТНБ            │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 МОДЕЛИ ДАННЫХ

### News (входная новость из БД ТНБ)

```python
class News:
    id: str                  # ID из БД ТНБ
    title: str              # Заголовок
    summary: str            # Краткое описание
    source: str             # Источник (Telegram канал)
    url: Optional[str]      # Ссылка на оригинал
    published_date: str     # ISO 8601 format
    category: Optional[str] # Категория (ai, marketplace, etc.)
```

### EnrichedNews (обогащенная через Perplexity)

```python
class EnrichedNews:
    # Базовые поля из News
    id: str
    title: str
    summary: str
    source: str

    # Обогащение от Perplexity
    enrichment: Enrichment
        additional_context: str      # Дополнительный контекст
        key_facts: List[str]         # 5-7 ключевых фактов
        background: str              # Предыстория события
        implications: str            # Возможные последствия
        related_topics: List[str]    # Связанные темы

    # Метаданные обработки
    processing_metadata: Metadata
        processed_at: str            # Timestamp обработки
        tokens_used: int             # Количество токенов
        model: str                   # Модель Perplexity
```

### ReelsScenario (сценарий для видео)

```python
class ReelsScenario:
    news_id: str                # ID исходной новости
    title: str                  # Заголовок видео
    duration: int = 30          # Длительность (фиксированно 30 сек)

    # Структура сценария
    script: Script
        hook: str               # 0-3 сек: Захватывающее начало
        main_content: str       # 3-25 сек: Основной контент
        cta: str                # 25-30 сек: Call-to-action

    # Дополнительные рекомендации
    visual_suggestions: List[str]  # Идеи для визуала
    hashtags: List[str]            # 5-7 хэштегов
    music_mood: str                # Настроение музыки
    target_audience: str           # Целевая аудитория
```

---

## 🔧 ТЕХНИЧЕСКИЙ СТЕК

### Зависимости

```python
# Уже есть в ТНБ (переиспользуем)
python = "^3.10"
python-dotenv = "*"
pydantic = "^2.0"
telethon = "*"

# Добавляем для Reels модуля
requests = "^2.31.0"      # HTTP клиент
aiohttp = "^3.9.0"        # Асинхронные HTTP запросы
tenacity = "^8.0"         # Retry логика
```

### API

- **Perplexity API** (sonar-pro модель)
  - Документация: https://docs.perplexity.ai/
  - Rate limits: учитываются в клиенте
  - Стоимость: ~$1 за 1000 запросов

---

## ⚙️ КОНФИГУРАЦИЯ

### Переменные окружения (.env)

```bash
# Существующие в ТНБ
TELEGRAM_API_ID=...
TELEGRAM_API_HASH=...
GEMINI_API_KEY=...
MY_PERSONAL_ACCOUNT=@username

# Добавляем для Reels
PERPLEXITY_API_KEY=your_perplexity_key_here
```

### Профиль конфигурации (config/profiles/reels.yaml)

```yaml
profile: reels

# Наследуем базовые настройки из config/base.yaml
paths:
  data_dir: ./data
  logs_dir: ./logs

# Perplexity настройки
perplexity:
  api_key: ${PERPLEXITY_API_KEY}
  model: sonar-pro
  timeout: 60
  max_retries: 3

# Обработка новостей
reels_processor:
  source: database              # database | file
  news_limit: 10                # Количество новостей для обработки
  filter_by_category: []        # Фильтр по категориям (пусто = все)

  # Промпты
  prompts:
    enrich_news: reels/prompts/enrich_news.md
    generate_reels: reels/prompts/generate_reels.md

  # Авто-запуск
  auto_run_after_processor: false  # true = запускать автоматически после processor

# Вывод результатов
output:
  telegram:
    enabled: true
    channel: ${MY_PERSONAL_ACCOUNT}  # Куда отправлять сценарии
    format: detailed                  # detailed | compact

  file:
    enabled: false
    path: ./data/reels_output
    formats: [json]                   # json | markdown

# Логирование
logging:
  level: INFO
  log_tokens: true   # Логировать использование токенов
```

---

## 🚀 ИСПОЛЬЗОВАНИЕ

### 1. Ручной запуск

```bash
# Обработать последние новости
python main.py reels --profile reels

# Обработать конкретное количество
python main.py reels --profile reels --limit 5

# Обработать только AI новости
python main.py reels --profile reels --category ai
```

### 2. Автоматический запуск после processor

```yaml
# config/profiles/reels.yaml
reels_processor:
  auto_run_after_processor: true
```

Теперь после `python main.py processor --profile ai` автоматически запустится reels generator.

### 3. Программный интерфейс

```python
from reels.services.perplexity_client import PerplexityClient
from reels.services.reels_processor import ReelsProcessor
from utils.config import load_config

# Инициализация
config = load_config("reels")
processor = ReelsProcessor(config)

# Обработка новостей
enriched_news, scenarios = await processor.process_latest_news(limit=10)

# Отправка в Telegram
await processor.send_to_moderation(scenarios)
```

---

## 📋 ОСНОВНЫЕ КОМПОНЕНТЫ

### 1. PerplexityClient

Клиент для взаимодействия с Perplexity API.

**Методы:**
- `enrich_news(news: News) -> EnrichedNews` — обогащение новости
- `generate_reels_scenario(enriched: EnrichedNews) -> ReelsScenario` — генерация сценария
- `_build_prompt(template: str, context: dict) -> str` — формирование промпта
- `_validate_response(response: dict) -> bool` — валидация JSON ответа

**Особенности:**
- Retry логика с экспоненциальной задержкой (3 попытки)
- Rate limiting (защита от превышения квот)
- Валидация JSON через Pydantic
- Логирование использования токенов

### 2. ReelsProcessor

Основной процессор для генерации Reels сценариев.

**Методы:**
- `process_latest_news(limit: int) -> tuple[List[EnrichedNews], List[ReelsScenario]]`
- `process_single_news(news: News) -> tuple[EnrichedNews, ReelsScenario]`
- `send_to_moderation(scenarios: List[ReelsScenario])` — отправка в Telegram
- `format_for_telegram(scenario: ReelsScenario) -> str` — форматирование

**Workflow:**
1. Получение новостей из БД ТНБ
2. Обогащение каждой через Perplexity
3. Генерация сценариев Reels
4. Форматирование для Telegram
5. Отправка на модерацию

### 3. Промпты

**enrich_news.md** — обогащение новости:
```
Ты эксперт-журналист. Изучи следующую новость и предоставь:

НОВОСТЬ:
{news_content}

ЗАДАЧА:
1. Найди дополнительную информацию и контекст
2. Выдели 5-7 ключевых фактов
3. Объясни предысторию события
4. Опиши возможные последствия
5. Укажи связанные темы

Ответ в JSON...
```

**generate_reels.md** — сценарий Reels:
```
Ты креатор вирусного контента для Instagram Reels.

ОБОГАЩЕННАЯ НОВОСТЬ:
{enriched_news}

ТРЕБОВАНИЯ:
- 30 секунд строго
- Hook (0-3): Захватывающее начало
- Content (3-25): Суть с фактами
- CTA (25-30): Призыв к действию

Ответ в JSON...
```

---

## 🔒 БЕЗОПАСНОСТЬ

- ✅ API ключ только через `.env`
- ✅ Валидация всех входных данных (Pydantic)
- ✅ Rate limiting для API запросов
- ✅ Sanitization перед отправкой в API
- ✅ Логирование без чувствительных данных

---

## 📊 МЕТРИКИ И МОНИТОРИНГ

Собираем и логируем:
- Количество обработанных новостей
- Использование токенов (input/output)
- Среднее время обработки одной новости
- Стоимость обработки (на основе токенов)
- Success rate API запросов
- Количество retry попыток

Пример лога:
```
INFO - Обработано новостей: 10
INFO - Токены: input=5420, output=3280, total=8700
INFO - Стоимость: $0.0087
INFO - Среднее время: 4.3 сек/новость
INFO - Success rate: 100%
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Unit тесты

```bash
# Все тесты
pytest reels/tests/

# Только Perplexity client
pytest reels/tests/test_perplexity_client.py

# С покрытием
pytest reels/tests/ --cov=reels --cov-report=html
```

### Тестовые данные

Используем моки для Perplexity API:
- `tests/fixtures/mock_api_responses.json` — примеры ответов
- `tests/fixtures/sample_news.json` — тестовые новости

---

## 📈 ROADMAP

См. подробную дорожную карту в `ROADMAP.md`

**Кратко:**
1. ✅ Описание проекта и архитектура
2. 🔨 Этап 1: Инфраструктура (2-3 часа)
3. 🔨 Этап 2: Perplexity клиент (2-3 часа)
4. 🔨 Этап 3: Reels процессор (3-4 часа)
5. 🔨 Этап 4: Интеграция в main.py (1-2 часа)
6. 🔨 Этап 5: Тестирование (2-3 часа)
7. 🔨 Этап 6: Документация (1-2 часа)

**Ожидаемый срок:** 11-17 часов разработки

---

## 💡 БУДУЩИЕ УЛУЧШЕНИЯ

- [ ] Поддержка YouTube Shorts (60 сек)
- [ ] Поддержка TikTok (15-60 сек)
- [ ] Экспорт в Markdown/PDF
- [ ] Web интерфейс для просмотра сценариев
- [ ] A/B тестирование промптов
- [ ] Интеграция с Instagram API для авто-постинга
- [ ] Кэширование обогащенных новостей

---

## 📞 КОНТАКТЫ И ПОДДЕРЖКА

- **GitHub**: https://github.com/lifeexplorer230/tg-news-bot
- **Issues**: https://github.com/lifeexplorer230/tg-news-bot/issues
- **Документация ТНБ**: См. `README.md` в корне проекта

---

**Создано с помощью Claude Code** 🤖
